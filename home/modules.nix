# Shareable modules for home-manager.
# This file should be a plain attr set of paths to modules.
# Directly imported to `homeModules` in flake.
{
  archLinux = ./arch-linux;
  features = ./features;
}
