# Personal Nix Configuration

## Structure

- `home/`: `home-manager` modules
- `nixos/`: NixOS modules (if I ever get to try it out...)

## Wishlist

- [ ] Figure out a nice way to specify users, hosts, and `homeConfigurations`.
- [ ] Move various user-level config to here.
  - [ ] Neovim
  - [ ] Various `systemd` configs
  - [ ] Figure out what more
- [ ] Try NixOS.
